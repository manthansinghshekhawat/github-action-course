name: building_blocks
on: workflow_dispatch
jobs:
  echo-hello:
    runs-on: ubuntu-latest
    steps:
      - name: say_hello
        run: echo "Hello World"
  echo-goodbye:
    runs-on: ubuntu-latest
    steps:
      - name: Failed steps
        run: |
          echo "I will Fail"
          exit 0
      - name: say_goodbye
        run: echo "goodbye"
